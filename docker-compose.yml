version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: dr_khare_postgres
    restart: always
    environment:
      POSTGRES_USER: drkhare
      POSTGRES_PASSWORD: drkhare_password
      POSTGRES_DB: drkhare_staging
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: dr_khare_redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redisdata:/data

  minio:
    image: minio/minio
    container_name: dr_khare_minio
    restart: always
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    command: server /data --console-address ":9001"
    volumes:
      - miniodata:/data

volumes:
  pgdata:
  redisdata:
  miniodata:
